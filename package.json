{
  "homepage": "https://github.com/Ariescat/Metis-Awesome/",
  "scripts": {
    "install": "npm install && gitbook install",
    "ginstall": "gitbook install",
    "gdev": "gitbook serve",
    "predeploy": "npm run build",
    "build": "gitbook build . ./_book",
    "deploy": "gh-pages -d _book"
  },
  "devDependencies": {
    "gh-pages": "^3.2.3",
    "gitbook-cli": "^2.3.2"
  }
}
